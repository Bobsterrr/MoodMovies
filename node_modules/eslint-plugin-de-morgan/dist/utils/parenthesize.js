'use strict'
Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' })
let parenthesize = (text, condition = true) => (condition ? `(${text})` : text)
exports.parenthesize = parenthesize
